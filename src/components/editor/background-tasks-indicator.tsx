"use client";

/**
 * @deprecated Replaced by the "Sistema" section inside the ActivityDrawer
 * (see `activity-drawer.tsx`). This component now renders nothing.
 *
 * Kept as a no-op for one release cycle so external imports don't break
 * in the middle of a deploy. Remove entirely once no references remain.
 */
export function BackgroundTasksIndicator() {
  return null;
}
